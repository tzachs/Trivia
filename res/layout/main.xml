<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#FFE8D0A9"
    android:orientation="vertical" >

    <Button
        android:id="@+id/buttonNewGameLevels"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/buttonNewGame"
        android:layout_margin="4dp"
        android:text="@string/buttonNewGameLevelsText" />

    <Button
        android:id="@+id/buttonNewGame"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_margin="4dp"
        android:text="@string/buttonNewGameText" />

    <Button
        android:id="@+id/buttonManageDatabase"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/buttonNewGame"
        android:layout_margin="4dp"
        android:text="@string/buttonManageDatabaseText" />
    

    <Button
        android:id="@+id/buttonPreferences"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/buttonManageDatabase"
        android:layout_margin="4dp"
        android:text="@string/buttonPreferencesText" />

  

</RelativeLayout>